{
  "name": "my_portfolio",
  "version": "1.1.0",
  "private": true,
  "description": "Personal portfolio site",
  "scripts": {
    "start": "npm run dev",
    "dev": "live-server --port=5500 --host=localhost"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
